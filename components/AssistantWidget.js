// This component has been intentionally disabled and removed from the application
// as part of a pre-deployment security audit to prevent exposure of API keys.
// The functionality will be reimplemented using a secure backend-for-frontend architecture.
const AssistantWidget = () => null;
export default AssistantWidget;
